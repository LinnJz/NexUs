先从基础重构QTabBar开始，帮我进行功能需求详细分析。
TabBar整体高度44，宽度不限，上下左右内边距是4，划分5个区域，分别是左侧固定工具栏，固定tab滚动区域，tab滚动区域，右侧固定工具栏，缩小放大关闭区域。
左侧固定工具栏

tab标签最长宽度220，最短宽度80，tab高度36，tab圆角4，tab内的icon大小20*20，icon距离tab标签左边9px，tab文字距离tab标签左边18px，tab内关闭图标由24*24矩形形成圆角4，关闭图标距离tab标签右侧5px，tab


举例整体设计划分区域，工具栏，固定tab滚动栏，tab滚动栏
分析tab标签添加删除，分析左侧固定工具按钮，右侧固定添加tab按钮，假设tab最大宽220，最小宽80，分析窗口缩放tab宽度缩放，分析tab标签过载后宽度缩小到80后，左右滚动按钮显示，分析左右滚动按钮启用状态根据tab索引，分析添加添加按钮后标签过载宽度缩小，分析添加tab、删除tab的抽屉动画，分析标签关闭后标签宽度不是立马反弹恢复，而是当鼠标移除tabbar视口或者关闭的标签是最后一个标签，宽度才能恢复，目的是为了能够动画执行完成后右侧tab被挤压到当前位置可以持续点击关闭标签按钮，分析工具按钮的右侧有一个42*42的区域，tab拖到这个位置会被固定，再拖动一个